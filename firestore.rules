rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PERMISOS TOTALES PARA DESARROLLO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
